# PostgreSQL Notify Adapter (postgres-js)

PostgreSQL LISTEN/NOTIFY via `@queuert/postgres` with the postgres-js client.

## Running

```bash
bun install
bun run --filter example-notify-postgres-postgres-js start
```

Requires Docker (uses testcontainers to start PostgreSQL).
