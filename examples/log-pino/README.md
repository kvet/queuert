# Pino Logging Example

Pino integration for structured logging, with per-job contextual loggers injected via `attemptMiddleware`.

## Running

```bash
bun install
bun run --filter example-log-pino start
```
