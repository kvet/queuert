# Queries Showcase

Read-only query methods for inspecting job chains and jobs.

Scenarios: single lookups with type narrowing, paginated lists, jobs within a chain ordered by chain index, blocker queries from both directions.

## Running

```bash
bun install
bun run --filter example-showcase-queries start
```
