# Runtime Validation with Zod

Custom validation adapter backed by Zod — covers nominal (by type name) and structural (by input shape) reference validation.

## Running

```bash
bun install
bun run --filter example-validation-zod start
```
