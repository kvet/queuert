# Winston Logging Example

Winston integration for structured logging, with per-job contextual loggers injected via `attemptMiddleware`.

## Running

```bash
bun install
bun run --filter example-log-winston start
```
