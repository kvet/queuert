---
"@queuert/dashboard": minor
---

Chain and job cards in the dashboard are now real links to their detail views, so you can right-click, middle-click, or ⌘/Ctrl-click a card to open it in a new tab (previously cards navigated only on a plain left click). The nested filter buttons and chain links inside each card stay individually clickable, and the card is keyboard-focusable.
